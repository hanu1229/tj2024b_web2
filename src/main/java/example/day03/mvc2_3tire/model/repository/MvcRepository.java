package example.day03.mvc2_3tire.model.repository;

import org.springframework.stereotype.Repository;

// MVC패턴의 Repository임을 알리는 어노테이션
// @Repository(JAP[DB매핑 라이브러리(JDBC기반)]) 실무
// @Mapper (마이바티스[DB연동 라이브러리(JDBC기반)]) 실무
// @Component (DAO[단순 JDBC 클래스])
// @Component (DAO[단순 JDBC 클래스])
@Repository
public interface MvcRepository {



}
